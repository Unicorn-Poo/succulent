pnpm exec lint-staged
# Type-check runs but won't block commits (Next.js ignores build errors)
# Run 'pnpm type-check' manually before deploying to catch errors
pnpm type-check || echo "⚠️  Type check failed - continuing anyway (run 'pnpm type-check' to see errors)"
